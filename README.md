# ds-kickoff-0926

Deck di presentazione, uso riservato.

Pagina non indicizzata: `noindex` a livello di meta tag e `Disallow: /` in `robots.txt`.
Non collegare questo URL da pagine pubbliche.

Sorgente di lavoro e documenti collegati restano nel workspace di agenzia, non qui.
Per aggiornare: sostituire `index.html` e fare push su `main`.
